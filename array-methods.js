/* 
  Мутабельные методы 

  // push() - добавить в конец
  const arr = [1, 2];
  arr.push(3); // [1, 2, 3]

  // pop() - удалить с конца
  const last = arr.pop(); // last = 3, arr = [1, 2]

  // unshift() - добавить в начало
  arr.unshift(0); // [0, 1, 2]

  // shift() - удалить с начала
  const first = arr.shift(); // first = 0, arr = [1, 2]

  // splice() - удалить/добавить элементы
  arr.splice(1, 1, 'a', 'b'); // удалить 1 элемент с индекса 1, добавить 'a','b'
  // arr = [1, 'a', 'b']

  изменение порядка

  // reverse() - перевернуть массив
  [1, 2, 3].reverse(); // [3, 2, 1]

  // sort() - сортировка
  [3, 1, 2].sort(); // [1, 2, 3]
  ['c', 'a', 'b'].sort(); // ['a', 'b', 'c']


  Иммутабельные методы:

  // map() - преобразовать каждый элемент
  [1, 2, 3].map(x => x * 2); // [2, 4, 6]

  // filter() - отфильтровать элементы
  [1, 2, 3, 4].filter(x => x % 2 === 0); // [2, 4]

  // flat() - "выпрямить" массив
  [1, [2, [3]]].flat(2); // [1, 2, 3]

  // flatMap() - map + flat
  [1, 2, 3].flatMap(x => [x, x * 2]); // [1, 2, 2, 4, 3, 6]

  // slice() - получить подмассив
  [1, 2, 3, 4].slice(1, 3); // [2, 3] (индексы 1 и 2)

  // concat() - объединить массивы
  [1, 2].concat([3, 4]); // [1, 2, 3, 4]


  Поиск элементов:

  // find() - найти первый подходящий элемент
  [1, 5, 10].find(x => x > 3); // 5

  // findIndex() - найти индекс первого подходящего элемента
  [1, 5, 10].findIndex(x => x > 3); // 1

  // indexOf() - найти индекс элемента
  ['a', 'b', 'c'].indexOf('b'); // 1

  // lastIndexOf() - найти последний индекс элемента
  ['a', 'b', 'a'].lastIndexOf('a'); // 2

  проверка условий:


  // includes() - проверить наличие элемента
  [1, 2, 3].includes(2); // true

  // some() - хотя бы один элемент удовлетворяет условию
  [1, 2, 3].some(x => x > 2); // true

  // every() - все элементы удовлетворяют условию
  [1, 2, 3].every(x => x > 0); // true

  Агрегация данных:

  // reduce() - свертка массива в одно значение
  [1, 2, 3].reduce((sum, x) => sum + x, 0); // 6

  // reduceRight() - свертка справа налево
  [1, 2, 3].reduceRight((sum, x) => sum + x, 0); // 6

  // join() - объединить элементы в строку
  ['a', 'b', 'c'].join('-'); // "a-b-c"


  методы перечисления и работы с каждым элементом:

  forEach, for-of, for-in, map, filter, while c условием,


  Статические методы Array:

  // Array.isArray() - проверка, является ли массивом
  Array.isArray([1, 2]); // true
  Array.isArray('not array'); // false

  // Array.from() - создать массив из итерируемого объекта
  Array.from('hello'); // ['h', 'e', 'l', 'l', 'o']
  Array.from({ length: 3 }, (_, i) => i); // [0, 1, 2]

  // Array.of() - создать массив из аргументов
  Array.of(1, 2, 3); // [1, 2, 3]
*/